/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images3.alphacoders.com",
      "toppng.com",
      "img.freepik.com",
      "pexels.com",
    ],
  },
};

export default nextConfig;
